# ServiceNow Exception Integration (Moved)

The ServiceNow integration guidance has been consolidated into the main `README.md` under the section:

- **"ServiceNow integration model (combined reference)"**

This file is intentionally retained as a compatibility pointer for existing links/bookmarks.
